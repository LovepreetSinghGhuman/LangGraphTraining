# Base dependencies for the RAG application
# Install with:
#   pip install -r config/requirements.txt
#
# This file contains the core packages needed for:
# - LangGraph orchestration and retrieval workflows
# - Vector store and embedding support
# - OCR and image processing features
# - Environment variable loading
#

# ── LangGraph & RAG stack ───────────────────────────────
langgraph
langchain-core
langchain-community
langchain-chroma
langchain-text-splitters
langchain-huggingface

# ── Embedding and model inference ───────────────────────
sentence-transformers
chromadb
transformers<5.9.0
accelerate

# ── Environment configuration ───────────────────────────
python-dotenv